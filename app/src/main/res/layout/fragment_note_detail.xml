<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="noteWrapper"
            type="mx.eduardopool.notes.models.wrappers.NoteWrapper" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/createDateTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{@string/label_created_at(noteWrapper.createDateFormatted)}"
            android:textAppearance="@style/TextAppearance.AppCompat.Caption" />

        <TextView
            android:id="@+id/noteDetail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@{noteWrapper.text}"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:textIsSelectable="true"
            tools:context=".fragments.NoteDetailFragment" />

    </LinearLayout>
</layout>